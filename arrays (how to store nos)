#include <stdio.h>

int main() {

    int marks[90];
    
    printf("entre the marks of 5 student:\n");
    
  /*scanf("%d",&marks[0]);
    scanf("%d",&marks[1]);
    scanf("%d",&marks[2]);
    scanf("%d",&marks[3]);
    scanf("%d",&marks[4]);
*/
   for(int i=0;i<5;i++){
       scanf("%d",&marks[i]);
   } 
    printf("the value of 0 and 1 is %d  %d",marks[0],marks[1]);

    return 0;
}
