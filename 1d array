#include <stdio.h>

int main() {
    
    int arr[10];
    printf("base address %p\n",arr);
    for(int i=0;i<10;i++)
      {
        printf("%d\t",(arr+i));
        printf("%d\t",(arr[i]));
        
    }

    return 0;
}
