#include <stdio.h>

int main() {
    int n,i=1; // we put 1 coz we need the value start form 1 
    scanf("%d",&n);
    do{
        printf("%d\n",i);
        i++;
    }while(i<=n);
    
    return 0;
}
