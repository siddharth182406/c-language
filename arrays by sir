#include<stdio.h>
int main()
{
    //datatype name[size];
    //string
    
    char name[]={"A null-terminated byte string (NTBS) is a sequence of nonzero bytes followed by a byte with value zero (the terminating null character). Each byte in a byte string encodes one character of some character set. "};
    
   /* printf("Enter your name:");

   // scanf("%s",name);
    
   // printf("%s",name);
    
    for (int i=0;i<13;i++)
    {
        
        scanf("%c",&name[i]);
    }*/
  //  printf("%s",name);
  int i=0;
  while (name[i]!='\0')
  {
      if (name[i]==' ')
      {
          printf("\n");
      }
      printf("%c",name[i++]);
  }
    return 0;
}
