#include<stdio.h>

void swap(int a, int b)
{
    printf("a=%d,b=%d\n",a,b) ;   
    int temp;
    temp=a;
    a=b;
    b=temp;
    printf("a=%d,b=%d\n",a,b) ;
}

int main()
{
    int x=10;
    int y=20;
    printf("x=%d,y=%d\n",x,y) ;
    swap(x,y);
    printf("x=%d,y=%d\n",x,y) ;
    
}
