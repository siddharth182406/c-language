#include <stdio.h>

int main() 
{
    float c, f;

    printf("Enter temperature in degree Celsius: ");
    scanf("%f", &c);

  
    f = (c * 9/5) + 32;

  
    printf(" C=%.2f to F=%.2f ", c, f);

    return 0;
}
